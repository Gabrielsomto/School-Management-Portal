import React from 'react';

const SynchronizeData = () => {
  return (
    <>
      SynchronizeData
      <button className="btn">Start Sync</button>
      <button className="btn margin-left">Sync Photos</button>

    </>
  );
}

export default SynchronizeData;
